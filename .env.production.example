# Production Environment Configuration for cPanel

# Server Configuration (Use standard port 80/443 via subdirectory)
NODE_ENV=production
PORT=3000

# Database Configuration (Updated with your cPanel details)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=sfbnfyhh_mbstore
DB_USER=sfbnfyhh_user
DB_PASSWORD=(Ua)NSS3mz

# JWT Secret (IMPORTANT: Change this to a secure random string)
JWT_SECRET=mbmode-production-jwt-secret-2026-change-this-secure-key

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# CORS Configuration (Updated with your actual domain + localhost for development)
CORS_ORIGIN=https://mbmodetlm.com,https://www.mbmodetlm.com,http://localhost:5173,http://localhost:3000

# Email Configuration (Optional - for order confirmations)
EMAIL_HOST=mail.mbmodetlm.com
EMAIL_PORT=587
EMAIL_USER=noreply@mbmodetlm.com
EMAIL_PASS=your_email_password